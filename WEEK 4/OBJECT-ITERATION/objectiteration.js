cosnt car = {
    brand: "Honda",
    model: "Civic",
    release: 1999,
    isManual: true,
    colour: "silver",
    wheels: 4,
};

let carKeys = Object.keys(cars);
console.log("Keys in the car object", carKeys);
console.log(carKeys.length);

carKeys.push("petrolType";)